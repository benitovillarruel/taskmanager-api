# taskmanager-api


Este es un proyecto básico en Python que implementa una función para sumar dos números y pruebas automáticas con pytest.

---

## 📂 Estructura

- `app/` → código principal
- `tests/` → pruebas automáticas

---

## ▶️ Instalación

Instalar dependencias:

```bash
pip install -r requirements.txt
